{
  "name": "nte-notes",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "editor": "node scripts/content-editor.mjs",
    "sync:translations": "node scripts/sync-translations.mjs",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^5.0.0",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "pagefind": "^1.1.1"
  }
}
